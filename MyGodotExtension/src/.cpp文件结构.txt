// 固定结构：读取 对应的.h文件
#include "（对应.h文件名）"

// 根据需要保留：读取需要的 C++ 文件中的类功能
#include <godot_cpp/（想要用的功能和它所在的位置）>
#include <godot_cpp/（想要用的功能和它所在的位置）>

// 固定结构：表示接下来的代码属于 godot 命名空间
using namespace godot;

// 固定结构：构造函数
（类名）::（类名（）） {
	// ==============================
	// 自己写：类刚被创建时要设置什么
	// 如果变量已经在.h中设置过，一般可以留空
	// ==============================
}


// 根据需要保留：析构函数
（类名）::（类名（）） {
    // 清理这个类使用的资源
}


// 根据需要保留：绑定Godot属性和函数
void （类名）::_bind_methods() {
	// 可以留空
}


// 节点准备完成时执行一次内容
void （类名）::（在.h中设置过的功能） {
	// 自己写：游戏开始时需要做什么
}


// 每个物理帧执行内容
void （类名）::（在.h中设置过的功能） {
	// ==============================
	// 从这里开始是写游戏逻辑
    // 例如：
	// 1. 取得当前速度
	// 2. 修改水平或垂直速度
	// 3. 把速度放回Player
	// 4. 调用move_and_slide()
	// ==============================

}